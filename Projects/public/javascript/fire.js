// incineroar
var _condition = "pokemon"
var _pocketmonster = "incineroar"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#incineroar');
    pokeIMG = `<img src="${img.front_default}" id="incineroar">`;
    selector.innerHTML = pokeIMG;
}
    );

    // charizard
var _condition = "pokemon"
var _pocketmonster = "charizard"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#charizard');
    pokeIMG = `<img src="${img.front_default}" id="charizard">`;
    selector.innerHTML = pokeIMG;
}
    );

      // moltres
var _condition = "pokemon"
var _pocketmonster = "moltres"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#moltres');
    pokeIMG = `<img src="${img.front_default}" id="moltres">`;
    selector.innerHTML = pokeIMG;
}
    );

       // charmander
var _condition = "pokemon"
var _pocketmonster = "charmander"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#charmander');
    pokeIMG = `<img src="${img.front_default}" id="charmander">`;
    selector.innerHTML = pokeIMG;
}
    );

     // charmeleon
var _condition = "pokemon"
var _pocketmonster = "charmeleon"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#charmeleon');
    pokeIMG = `<img src="${img.front_default}" id="charmeleon">`;
    selector.innerHTML = pokeIMG;
}
    );

     // vulpix
var _condition = "pokemon"
var _pocketmonster = "vulpix"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#vulpix');
    pokeIMG = `<img src="${img.front_default}" id="vulpix">`;
    selector.innerHTML = pokeIMG;
}
    );

     // ninetales
var _condition = "pokemon"
var _pocketmonster = "ninetales"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#ninetales');
    pokeIMG = `<img src="${img.front_default}" id="ninetales">`;
    selector.innerHTML = pokeIMG;
}
    );

     // growlithe
var _condition = "pokemon"
var _pocketmonster = "growlithe"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#growlithe');
    pokeIMG = `<img src="${img.front_default}" id="growlithe">`;
    selector.innerHTML = pokeIMG;
}
    );

        // arcanine
var _condition = "pokemon"
var _pocketmonster = "arcanine"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#arcanine');
    pokeIMG = `<img src="${img.front_default}" id="arcanine">`;
    selector.innerHTML = pokeIMG;
}
    );

     // ponyta
var _condition = "pokemon"
var _pocketmonster = "ponyta"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#ponyta');
    pokeIMG = `<img src="${img.front_default}" id="ponyta">`;
    selector.innerHTML = pokeIMG;
}
    );

      // rapidash
var _condition = "pokemon"
var _pocketmonster = "rapidash"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#rapidash');
    pokeIMG = `<img src="${img.front_default}" id="rapidash">`;
    selector.innerHTML = pokeIMG;
}
    );

         // magmar
var _condition = "pokemon"
var _pocketmonster = "magmar"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#magmar');
    pokeIMG = `<img src="${img.front_default}" id="magmar">`;
    selector.innerHTML = pokeIMG;
}
    );

         // flareon
var _condition = "pokemon"
var _pocketmonster = "flareon"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#flareon');
    pokeIMG = `<img src="${img.front_default}" id="flareon">`;
    selector.innerHTML = pokeIMG;
}
    );

     // braixen
var _condition = "pokemon"
var _pocketmonster = "braixen"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#braixen');
    pokeIMG = `<img src="${img.front_default}" id="braixen">`;
    selector.innerHTML = pokeIMG;
}
    );

     // litten
var _condition = "pokemon"
var _pocketmonster = "litten"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#litten');
    pokeIMG = `<img src="${img.front_default}" id="litten">`;
    selector.innerHTML = pokeIMG;
}
    );

     // flareon
var _condition = "pokemon"
var _pocketmonster = "flareon"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#flareon');
    pokeIMG = `<img src="${img.front_default}" id="flareon">`;
    selector.innerHTML = pokeIMG;
}
    );

     // torracat
var _condition = "pokemon"
var _pocketmonster = "torracat"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#torracat');
    pokeIMG = `<img src="${img.front_default}" id="torracat">`;
    selector.innerHTML = pokeIMG;
}
    );

     // cyndaquil
var _condition = "pokemon"
var _pocketmonster = "cyndaquil"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#cyndaquil');
    pokeIMG = `<img src="${img.front_default}" id="cyndaquil">`;
    selector.innerHTML = pokeIMG;
}
    );

      //quilava 
var _condition = "pokemon"
var _pocketmonster = "quilava"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#quilava');
    pokeIMG = `<img src="${img.front_default}" id="quilava">`;
    selector.innerHTML = pokeIMG;
}
    );

      // typhlosion
var _condition = "pokemon"
var _pocketmonster = "typhlosion"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#typhlosion');
    pokeIMG = `<img src="${img.front_default}" id="typhlosion">`;
    selector.innerHTML = pokeIMG;
}
    );

      // slugma
var _condition = "pokemon"
var _pocketmonster = "slugma"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#slugma');
    pokeIMG = `<img src="${img.front_default}" id="slugma">`;
    selector.innerHTML = pokeIMG;
}
    );

      // magcargo
var _condition = "pokemon"
var _pocketmonster = "magcargo"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#magcargo');
    pokeIMG = `<img src="${img.front_default}" id="magcargo">`;
    selector.innerHTML = pokeIMG;
}
    );

      // houndour
var _condition = "pokemon"
var _pocketmonster = "houndour"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#houndour');
    pokeIMG = `<img src="${img.front_default}" id="houndour">`;
    selector.innerHTML = pokeIMG;
}
    );

      // houndoom
var _condition = "pokemon"
var _pocketmonster = "houndoom"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#houndoom');
    pokeIMG = `<img src="${img.front_default}" id="houndoom">`;
    selector.innerHTML = pokeIMG;
}
    );

    // magby
var _condition = "pokemon"
var _pocketmonster = "magby"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#magby');
    pokeIMG = `<img src="${img.front_default}" id="magby">`;
    selector.innerHTML = pokeIMG;
}
    );

     // entei
var _condition = "pokemon"
var _pocketmonster = "entei"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#entei');
    pokeIMG = `<img src="${img.front_default}" id="entei">`;
    selector.innerHTML = pokeIMG;
}
    );

     // ho-oh
var _condition = "pokemon"
var _pocketmonster = "ho-oh"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#ho-oh');
    pokeIMG = `<img src="${img.front_default}" id="ho-oh">`;
    selector.innerHTML = pokeIMG;
}
    );

     // torchic
var _condition = "pokemon"
var _pocketmonster = "torchic"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#torchic');
    pokeIMG = `<img src="${img.front_default}" id="torchic">`;
    selector.innerHTML = pokeIMG;
}
    );

     // combusken
var _condition = "pokemon"
var _pocketmonster = "combusken"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#combusken');
    pokeIMG = `<img src="${img.front_default}" id="combusken">`;
    selector.innerHTML = pokeIMG;
}
    );

     // blaziken
var _condition = "pokemon"
var _pocketmonster = "blaziken"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#blaziken');
    pokeIMG = `<img src="${img.front_default}" id="blaziken">`;
    selector.innerHTML = pokeIMG;
}
    );

 // numel
 var _condition = "pokemon"
 var _pocketmonster = "numel"
 
 fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
 .then( res => res.json())
 .then(data => {
     console.log(data.sprites)
     const {...img} = data.sprites;
     selector = document.querySelector('#numel');
     pokeIMG = `<img src="${img.front_default}" id="numel">`;
     selector.innerHTML = pokeIMG;
 }
     );
      // camerupt
var _condition = "pokemon"
var _pocketmonster = "camerupt"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#camerupt');
    pokeIMG = `<img src="${img.front_default}" id="camerupt">`;
    selector.innerHTML = pokeIMG;
}
    );

     // torkoal
var _condition = "pokemon"
var _pocketmonster = "torkoal"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#torkoal');
    pokeIMG = `<img src="${img.front_default}" id="torkoal">`;
    selector.innerHTML = pokeIMG;
}
    );

     // chimchar
var _condition = "pokemon"
var _pocketmonster = "chimchar"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#chimchar');
    pokeIMG = `<img src="${img.front_default}" id="chimchar">`;
    selector.innerHTML = pokeIMG;
}
    );

     // monferno
var _condition = "pokemon"
var _pocketmonster = "monferno"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#monferno');
    pokeIMG = `<img src="${img.front_default}" id="monferno">`;
    selector.innerHTML = pokeIMG;
}
    );

     // infernape
var _condition = "pokemon"
var _pocketmonster = "infernape"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    selector = document.querySelector('#infernape');
    pokeIMG = `<img src="${img.front_default}" id="infernape">`;
    selector.innerHTML = pokeIMG;
}
    );

