// gastly
  var _condition = "pokemon"
  var _pocketmonster = "gastly"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#gastly');
      pokeIMG = `<img src="${img.front_default}" id="gastly">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // haunter
  var _condition = "pokemon"
  var _pocketmonster = "haunter"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#haunter');
      pokeIMG = `<img src="${img.front_default}" id="haunter">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // gengar
  var _condition = "pokemon"
  var _pocketmonster = "gengar"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#gengar');
      pokeIMG = `<img src="${img.front_default}" id="gengar">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // misdreavus
  var _condition = "pokemon"
  var _pocketmonster = "misdreavus"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#misdreavus');
      pokeIMG = `<img src="${img.front_default}" id="misdreavus">`;
      selector.innerHTML = pokeIMG;
  }
      );

        // shuppet
  var _condition = "pokemon"
  var _pocketmonster = "shuppet"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#shuppet');
      pokeIMG = `<img src="${img.front_default}" id="shuppet">`;
      selector.innerHTML = pokeIMG;
  }
      );

        // banette
  var _condition = "pokemon"
  var _pocketmonster = "banette"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#banette');
      pokeIMG = `<img src="${img.front_default}" id="banette">`;
      selector.innerHTML = pokeIMG;
  }
      );

        // duskull
  var _condition = "pokemon"
  var _pocketmonster = "duskull"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#duskull');
      pokeIMG = `<img src="${img.front_default}" id="duskull">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // dusclops
  var _condition = "pokemon"
  var _pocketmonster = "dusclops"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#dusclops');
      pokeIMG = `<img src="${img.front_default}" id="dusclops">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // mismagius
  var _condition = "pokemon"
  var _pocketmonster = "mismagius"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#mismagius');
      pokeIMG = `<img src="${img.front_default}" id="mismagius">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // dusknoir
  var _condition = "pokemon"
  var _pocketmonster = "dusknoir"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#dusknoir');
      pokeIMG = `<img src="${img.front_default}" id="dusknoir">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // yamask
  var _condition = "pokemon"
  var _pocketmonster = "yamask"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#yamask');
      pokeIMG = `<img src="${img.front_default}" id="yamask">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // cofagrigus
  var _condition = "pokemon"
  var _pocketmonster = "cofagrigus"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#cofagrigus');
      pokeIMG = `<img src="${img.front_default}" id="cofagrigus">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // drifloon
  var _condition = "pokemon"
  var _pocketmonster = "drifloon"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#drifloon');
      pokeIMG = `<img src="${img.front_default}" id="drifloon">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // drifblim
  var _condition = "pokemon"
  var _pocketmonster = "drifblim"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#drifblim');
      pokeIMG = `<img src="${img.front_default}" id="drifblim">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // spiritomb
  var _condition = "pokemon"
  var _pocketmonster = "spiritomb"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#spiritomb');
      pokeIMG = `<img src="${img.front_default}" id="spiritomb">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // litwick2
  var _condition = "pokemon"
  var _pocketmonster = "litwick"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#litwick2');
      pokeIMG = `<img src="${img.front_default}" id="litwick2">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // lampent2
  var _condition = "pokemon"
  var _pocketmonster = "lampent"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#lampent2');
      pokeIMG = `<img src="${img.front_default}" id="lampent2">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // chandelure2
  var _condition = "pokemon"
  var _pocketmonster = "chandelure"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#chandelure2');
      pokeIMG = `<img src="${img.front_default}" id="chandelure2">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // shedinja
  var _condition = "pokemon"
  var _pocketmonster = "shedinja"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#shedinja');
      pokeIMG = `<img src="${img.front_default}" id="shedinja">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // sableye
  var _condition = "pokemon"
  var _pocketmonster = "sableye"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#sableye');
      pokeIMG = `<img src="${img.front_default}" id="sableye">`;
      selector.innerHTML = pokeIMG;
  }
      );

         // froslass
  var _condition = "pokemon"
  var _pocketmonster = "froslass"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#froslass');
      pokeIMG = `<img src="${img.front_default}" id="froslass">`;
      selector.innerHTML = pokeIMG;
  }
      );

         // rotom
  var _condition = "pokemon"
  var _pocketmonster = "rotom"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#rotom');
      pokeIMG = `<img src="${img.front_default}" id="rotom">`;
      selector.innerHTML = pokeIMG;
  }
      );

         // frillish
  var _condition = "pokemon"
  var _pocketmonster = "frillish"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#frillish');
      pokeIMG = `<img src="${img.front_default}" id="frillish">`;
      selector.innerHTML = pokeIMG;
  }
      );

         // jellicent
  var _condition = "pokemon"
  var _pocketmonster = "jellicent"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#jellicent');
      pokeIMG = `<img src="${img.front_default}" id="jellicent">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // golett
  var _condition = "pokemon"
  var _pocketmonster = "golett"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#golett');
      pokeIMG = `<img src="${img.front_default}" id="golett">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // golurk
  var _condition = "pokemon"
  var _pocketmonster = "golurk"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#golurk');
      pokeIMG = `<img src="${img.front_default}" id="golurk">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // honedge
  var _condition = "pokemon"
  var _pocketmonster = "honedge"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#honedge');
      pokeIMG = `<img src="${img.front_default}" id="honedge">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // doublade
  var _condition = "pokemon"
  var _pocketmonster = "doublade"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#doublade');
      pokeIMG = `<img src="${img.front_default}" id="doublade">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // trevenant
  var _condition = "pokemon"
  var _pocketmonster = "trevenant"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#trevenant');
      pokeIMG = `<img src="${img.front_default}" id="trevenant">`;
      selector.innerHTML = pokeIMG;
  }
      );

       // hoopa
  var _condition = "pokemon"
  var _pocketmonster = "hoopa"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#hoopa');
      pokeIMG = `<img src="${img.front_default}" id="hoopa">`;
      selector.innerHTML = pokeIMG;
  }
      );

      // decidueye
  var _condition = "pokemon"
  var _pocketmonster = "decidueye"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#decidueye');
      pokeIMG = `<img src="${img.front_default}" id="decidueye">`;
      selector.innerHTML = pokeIMG;
  }
      );

         // sandygast
  var _condition = "pokemon"
  var _pocketmonster = "sandygast"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#sandygast');
      pokeIMG = `<img src="${img.front_default}" id="sandygast">`;
      selector.innerHTML = pokeIMG;
  }
      );

         // palossand
  var _condition = "pokemon"
  var _pocketmonster = "palossand"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#palossand');
      pokeIMG = `<img src="${img.front_default}" id="palossand">`;
      selector.innerHTML = pokeIMG;
  }
      );

         // dhelmise
  var _condition = "pokemon"
  var _pocketmonster = "dhelmise"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#dhelmise');
      pokeIMG = `<img src="${img.front_default}" id="dhelmise">`;
      selector.innerHTML = pokeIMG;
  }
      );

         // lunala
  var _condition = "pokemon"
  var _pocketmonster = "lunala"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#lunala');
      pokeIMG = `<img src="${img.front_default}" id="lunala">`;
      selector.innerHTML = pokeIMG;
  }
      );

         // marshadow
  var _condition = "pokemon"
  var _pocketmonster = "marshadow"
  
  fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
  .then( res => res.json())
  .then(data => {
      console.log(data.sprites)
      const {...img} = data.sprites;
      selector = document.querySelector('#marshadow');
      pokeIMG = `<img src="${img.front_default}" id="marshadow">`;
      selector.innerHTML = pokeIMG;
  }
      );